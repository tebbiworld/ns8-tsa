# Paths included in the module backup (state/environment is always included).
#   state/passwords.env   password of the encrypted signing key
#   state/certs           signing key, certificate chain and server config
#   state/cert-archive    previous certificate generations (old timestamps
#                         are verified against them)
state/passwords.env
state/certs
state/cert-archive
