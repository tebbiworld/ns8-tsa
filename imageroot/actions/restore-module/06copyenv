#!/usr/bin/env python3

#
# Copyright (C) 2026 tebbi
# SPDX-License-Identifier: GPL-3.0-or-later
#

# Copy the non-secret settings of the original instance. The key password comes
# back with the restored state/passwords.env; a backup taken with an older
# module version still carries it in the environment.

import json
import sys

import agent
import modsecrets

request = json.load(sys.stdin)
env = request["environment"]

KEEP = ["TRAEFIK_HOST", "TRAEFIK_HTTP2HTTPS", "TRAEFIK_LETS_ENCRYPT", "TSA_ROOT_CA_CN", "TSA_LEAF_CN"]

agent.mset_env({k: env[k] for k in KEEP if k in env})

if env.get("TSA_KEY_PASSWORD"):
    modsecrets.write({"TSA_KEY_PASSWORD": env["TSA_KEY_PASSWORD"]})
    print("restored the legacy key password from the backup environment into passwords.env", file=sys.stderr)
